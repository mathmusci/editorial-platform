# ADR-0002: Use `src/editorial` as the Python package layout

**Status:** Accepted

The repository uses a `src` layout with a single root package: `src/editorial/`.
The distribution name remains `editorial-platform`.
