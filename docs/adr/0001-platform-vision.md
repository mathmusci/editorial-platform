# ADR-0001: The platform is a configurable editorial processing engine

**Status:** Accepted

The core package is domain-agnostic. Publications such as the BIS newsletter are configuration over the platform.
